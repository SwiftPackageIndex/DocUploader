import XCTest
@testable import spi_doc_uploader

final class spi_doc_uploaderTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(spi_doc_uploader().text, "Hello, World!")
    }
}
